name: Publish to PyPI

on:
  push:
    branches: [main]
    paths: [pyproject.toml]

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install version checker
        run: python -m pip install packaging

      - name: Check version bump
        id: version
        run: |
          CURRENT=$(python -c "
          import tomllib
          with open('pyproject.toml', 'rb') as f:
              print(tomllib.load(f)['project']['version'])
          ")
          if git cat-file -e HEAD~1:pyproject.toml 2>/dev/null; then
            PREVIOUS=$(git show HEAD~1:pyproject.toml | python -c "
          import tomllib, sys
          print(tomllib.load(sys.stdin.buffer)['project']['version'])
            ")
          else
            PREVIOUS=""
          fi
          echo "current=$CURRENT" >> "$GITHUB_OUTPUT"
          echo "previous=$PREVIOUS" >> "$GITHUB_OUTPUT"
          if [ -z "$PREVIOUS" ]; then
            echo "bumped=true" >> "$GITHUB_OUTPUT"
          elif [ "$CURRENT" != "$PREVIOUS" ]; then
            python -c "
          from packaging.version import Version
          current, previous = Version('$CURRENT'), Version('$PREVIOUS')
          exit(0 if current > previous else 1)
          "
            echo "bumped=true" >> "$GITHUB_OUTPUT"
          else
            echo "bumped=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        if: steps.version.outputs.bumped == 'true'
        run: |
          python -m pip install --upgrade build
          for attempt in 1 2 3 4 5; do
            if python -m pip install --no-cache-dir --editable ".[dev]"; then
              break
            fi
            if [ "$attempt" -eq 5 ]; then
              exit 1
            fi
            sleep 30
          done
          black --check src/hvbrowser tests
          ruff check src/hvbrowser tests
          mypy src/hvbrowser
          pytest
          python -m build

      - name: Publish
        if: steps.version.outputs.bumped == 'true'
        uses: pypa/gh-action-pypi-publish@release/v1
